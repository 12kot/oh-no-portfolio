import React, { ReactElement } from "react";
import styles from "./contacts.module.scss";

const Contacts = (): ReactElement => {
  return <main className={styles.container} id="contacts">Contacts</main>;
};

export default Contacts;
