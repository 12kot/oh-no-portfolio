import React, { ReactElement } from "react";
import styles from "./links.module.scss";

const Links = (): ReactElement => {
  return <section className={styles.links}>Links</section>;
};

export default Links;
